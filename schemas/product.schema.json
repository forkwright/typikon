{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/forkwright/typikon/schemas/product.schema.json",
  "title": "typikon product",
  "description": "Frontmatter contract for a product page (content/products/<slug>.md). A recorded price may render as catalog data, but checkout requires sourced purchasable availability and shipping text requires its own source.",
  "type": "object",
  "required": ["title", "description", "extra"],
  "additionalProperties": false,
  "properties": {
    "title": { "type": "string", "minLength": 1, "maxLength": 80 },
    "description": { "type": "string", "minLength": 30, "maxLength": 200 },
    "date": { "type": "string", "format": "date" },
    "updated": { "type": "string", "format": "date" },
    "path": { "type": "string", "pattern": "^/[a-z0-9/_-]*/?$" },
    "template": { "type": "string", "pattern": "^[a-z0-9_-]+\\.html$" },
    "weight": { "type": "integer", "minimum": 0 },
    "draft": { "type": "boolean" },
    "include_in_feeds": { "type": "boolean", "description": "Whether Zola may include this dated product in generated feeds. Defaults to true." },
    "extra": {
      "type": "object",
      "required": ["audience", "price", "price_source"],
      "additionalProperties": false,
      "properties": {
        "audience": {
          "type": "string",
          "minLength": 3,
          "maxLength": 120,
          "description": "Intended buyer or reader context for the product page."
        },
        "price": {
          "type": "string",
          "pattern": "^\\$?\\d{1,5}(\\.\\d{2})?$",
          "description": "Display price with optional leading $ (e.g. '$150', '85'). Bare number assumed USD."
        },
        "price_source": {
          "type": "string",
          "minLength": 3,
          "maxLength": 200,
          "description": "Source for the rendered price claim, e.g. the Stripe price object, SKU ledger, or fixture note."
        },
        "stripe_url": {
          "type": "string",
          "pattern": "^https://buy\\.stripe\\.com/[A-Za-z0-9_]+$",
          "description": "Stripe direct-checkout URL. Optional catalog metadata; when present, the schema requires sourced purchasable availability before templates may emit it."
        },
        "shipping_note": {
          "type": "string",
          "maxLength": 200,
          "description": "Public shipping or lead-time claim. Rendered only alongside shipping_source; no default is invented."
        },
        "shipping_source": {
          "type": "string",
          "minLength": 3,
          "maxLength": 200,
          "description": "Source for the shipping or lead-time claim. Required alongside shipping_note."
        },
        "availability": {
          "type": "string",
          "enum": ["InStock", "OutOfStock", "PreOrder", "BackOrder", "LimitedAvailability", "Discontinued"],
          "description": "Schema.org Offer.availability state. InStock, PreOrder, BackOrder, and LimitedAvailability permit checkout only alongside availability_source. Other or absent states are catalog-only."
        },
        "availability_source": {
          "type": "string",
          "minLength": 3,
          "maxLength": 200,
          "description": "Source for the availability claim, e.g. inventory count or a manual stock check. Required alongside availability."
        },
        "seo_title": { "type": "string", "maxLength": 80 },
        "body_class": { "type": "string", "pattern": "^[a-z][a-z0-9 -]*$" },
        "og_image": { "type": "string", "pattern": "^[a-z0-9/_-]+\\.(svg|png|jpg|jpeg|webp)$" },
        "og_type": { "type": "string", "enum": ["website", "article", "profile"] },
        "skip_sitemap": { "type": "boolean", "description": "Exclude from sitemap.xml." },
        "author": { "type": "string", "description": "Per-product author override." },
        "images": {
          "type": "array",
          "description": "Product photography. First image becomes the hero (eager-loaded); the rest lazy-load. Files live under static/<src>; Zola's resize_image() generates 400/800/1200px WebP variants automatically. The og_image field is independent — typically points at the same hero shot or a 1200x630 social-card variant.",
          "items": {
            "type": "object",
            "required": ["src", "alt"],
            "additionalProperties": false,
            "properties": {
              "src": {
                "type": "string",
                "pattern": "^[a-z0-9/_-]+\\.(jpg|jpeg|png|webp|tiff|heic)$",
                "description": "Path under static/ (e.g. 'img/products/belt/01.jpg'). Camera-native formats (heic, tiff) accepted as input; resize_image converts to WebP."
              },
              "alt": {
                "type": "string",
                "minLength": 5,
                "maxLength": 200,
                "description": "Accessible description. Describe what's in the photo for visitors who can't see it."
              },
              "caption": {
                "type": "string",
                "maxLength": 200,
                "description": "Optional figcaption rendered under the image."
              }
            }
          }
        }
      },
      "dependentRequired": {
        "availability": ["availability_source"],
        "availability_source": ["availability"],
        "shipping_note": ["shipping_source"],
        "shipping_source": ["shipping_note"]
      },
      "allOf": [
        {
          "if": { "required": ["stripe_url"] },
          "then": {
            "required": ["availability", "availability_source"],
            "properties": {
              "availability": {
                "enum": ["InStock", "PreOrder", "BackOrder", "LimitedAvailability"]
              }
            }
          }
        }
      ]
    }
  }
}
